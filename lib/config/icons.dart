import 'package:trickle/data/local/assets.dart';
import 'package:trickle/widgets/custom_icon.dart';

class I {
  // Common
  static final flowMove = CustomIconData.single(A.icon('flow_move'));
  static final flowCopy = CustomIconData.single(A.icon('flow_copy'));
}
